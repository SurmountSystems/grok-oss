# Skill pin — plan / review / design Hard stop (2026-07-24)

plan/review/design: **touched** — added § Hard stop (spawn-first on CI; parent coordinator only; join on disk; link `~/.grok/AGENTS.md`). shared/references/subagent-token-strategy.md + upstream-export-import: **already-ok**.
