/target
**/*.rs.bk
.DS_Store
result
/result

# Nested crate cargo targets (standalone crates like cargo-mem-guard)
**/target/

# Free-GHA just ci outer-loop marker (touched mid-recipe; never commit)
.ci-started

# Agent reports stay on this machine. Do not add them to the git tree.
.agents/reports/

# PTY harness / interactive shells must not leak history into crates.
**/.bash_history
