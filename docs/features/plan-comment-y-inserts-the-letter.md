# Plan comment inserts the letter y

Source: screenshot Fri Sep 25, 2026, 5:03 AM, plan comment on lines 10-12.

The footer the Operator saw was `Enter:save comment | y:copy | Esc:cancel`. They typed in the focused plan comment composer. The line came out as `for ou too` because bare y did not insert. They can type y in the main composer.

While the plan comment composer is focused, y is text. Copy stays the Copy button, or a chord that is not the bare letter y. Bare y must not copy while that box is focused. Enter still saves the comment. Esc still cancels. The footer must not advertise `y:copy` during that text entry.

Empty Preview may still copy the plan with bare y. That path is not this box.

Status: while that box is focused, bare y inserts. The footer is `Enter:save comment | Esc:cancel`. It does not advertise `y:copy`. Enter still saves. Esc still cancels. Empty Preview still copies with bare y. Copy while commenting stays the Copy button.
