# FORK defense inventory for the next upstream merge

**Date:** 2026-08-15  
**Role:** L2 closeout from L3 reports only. This file does not re-walk the tree.

Sources: `fork-docs-map.md`, `fork-docs-tools-map.md`, `fork-docs-seams.md`, `fork-docs-process-gaps.md`, `fork-docs-fork-write.md`, `fork-docs-process-write.md`, `fork-docs-review.md`, `fork-docs-fix.md`, `fork-docs-mop.md`.

SuperGrok is a **paid** product. Meters stay distinct: included SuperGrok period limits, SuperGrok dollar credits, console team prepaid / console API credits.

## What FORK now defends that it did not

`FORK.md` is a hierarchical defense inventory again, not a checkbox list.

**Kept:** Vision, Git flow, Remotes, Syncing with xAI.

**Product / Chrome one-liners now carry named `fn`s** for seams that were missing, one-line, or buried in Process:

- CLI identity plus welcome / tutorial / hero **Grok OSS** badges
- Last-session on start, including Welcome / headless / ctx-flag siblings
- Dual-auth hop after included SuperGrok period limits are full, Business/Team included before personal, sibling included before SuperGrok dollar credits, one-process limits flock, combined remaining
- Token Economy ledger `/spend` ingest of `usage.jsonl` (kept distinct from the append-only usage log)
- Unread config plus `/settings` rows and runtime readers
- DOGE chrome: human green rails and caret, magenta agent rail, titled composer, compact included SuperGrok period limits meter
- Always-on bubble copy **click** plus wrap when the first line is full (paint-only is failed land)
- Plan five-CTA, present is not Approve, modal-free typing
- Pause / resume / stop chips and Clear finished quiet paint
- Skills sanitize / extract / roots plus three Rust intercepts
- Always-three-layer **product** prompt (`child_task_description_is_concise`, `default_max_allows_l2_to_spawn_l3`) plus process pin in AGENTS
- `from_config` no-prefetch usable catalog
- `/rebuild` SHA-aware peer relaunch (not only fail-does-not-signal)
- Nucleo reuse-per-root
- User-guide fork pins as one line + link, plus cargo pins for hop-shipped and spend order

**Land checklist** no longer mixes process notes into the seven product class count. Classes match the catalog: (1) CLI identity, (2) config is a surface, (3) Token Economy ledger `/spend`, (4) DOGE/chrome paint, (5) dual-auth hop, (6) last-session, (7) product skills are not a Python runtime. Chrome-only, paint-only bubble copy, and skills Python reintroduced are failed land. A short **How to defend a seam** section states: FORK line + named test + catalog row + cherry-pick; import restores docs only; `just check` cannot fail a deleted catalog test.

Independent review: **pass-with-nits**, then the class 5 operator cheat sheet hole was fixed.

## What process tools changed

One catalog, one path assert. No second inventory file. No D1 AGENTS novel.

| Tool | Change |
|------|--------|
| `doc/dev/upstream-regression-filters.md` | Enrolled proven extras. Removed no-`fn` names from required land. Operator cheat sheet class 5 now matches Required land §5 plus §5b. Class 3 heading still starts with `### 3. grok-oss SQL extras` and says Token Economy ledger, not SuperGrok dollar credits. |
| `~/.agents/skills/git-recon/SKILL.md` | Seven product classes. Always-three-layer (no "many greps"). Chrome-only closeout refused. Extra neighbors walked if listed (bubble click, plan honesty, SHA rebuild, nucleo, `from_config`, pause / Clear finished, three-layer product prompt, user-guide hop / spend-order). |
| `~/.agents/skills/upstream-export-import/SKILL.md` | Same seven-class dual-pin. |
| `docs/upstream-history.md` | HITL / import review: walk FORK classes, prove catalog named tests exist. |
| `scripts/assert-process-pins.sh` | Catalog file must exist. Seven class titles must appear in the catalog (worktree and tree-ish). Still no cargo. Still not contract proof. |
| `scripts/recon-status.sh` | Land next-action names assert **and** walk FORK/catalog named tests. `just check` is quality only. |
| `scripts/import-upstream-export.sh` | Post-restore NOTE prints the seven classes and the catalog path. |
| `justfile` | `just upstream-land-filters` runs path assert then prints the seven-class reminder. Does not run cargo. Does not replace `just check`. |

**Mop:** no `*.rs` in this slice. Worktree `./scripts/assert-process-pins.sh` exit **0**. `just upstream-land-filters` exit **0**. Tree-ish `assert HEAD` exit **1** because committed HEAD still has the old catalog without the seven titles. That is the new sniff working, not a worktree mop. After this catalog is on the tree being landed, the same markers should pass.

## Named leftovers that are out of this ask

- Operator-gated rebuild / quit / live TUI dogfood. Source restore is not a rebuilt `grok-oss` on the machine.
- Host `~/.grok/docs` extract staying stale until the next product launch.
- C4 (server included SuperGrok period debit) is an xAI ticket, not a restack land class.
- Process features parked for later plans (agentic fmt/clippy via ACP, thoughtful todo tracking, structured conversation, Sapient Experience product steers).

## Honesty: still unproven, already labeled

These are documented as unproven. They are **not** cargo land classes.

- rustc 1.97.1 / fenix match (file pin; `rust-toolchain.toml` is not in `FORK_PATHS`)
- Empty `models_cache.json` miss (no dedicated `fn`; only no-prefetch usable catalog is proven)
- Nucleo pool `Some(2)` (constant only; reuse-per-root is proven)
- User-guide `/limits` hit-count; last-session guide sentences; three-layer guide paragraph (text exists, no dedicated `fn`)
- Stuck-retry **pager** chrome; `shell_collision` / pager `SHELL_RESERVED`; missing window-title `fn`s; lower-left throbber **color** `fn`s
- Token Economy / economic-mode / auto-run `/settings` GUI rows (not re-proven)
- Session recap / cancel-subagents Settings e2e; `[subagents] allow_worktree` actually changing spawn isolation
- Host `~/.agents/skills` as a product land class (it is not)

A chrome-only 1.0.3 land **fails** if the agent follows FORK, the catalog Required land / cheat sheet, and `git-recon` `recon:land`. Assert plus `just check` alone would still pass on a chrome-only tree. That closeout is now written as a failed land, not as success.

No git. No live TUI rebuild.

End of L2 closeout.
