# Recon / land process write (2026-08-15)

One catalog, one path assert, host git-recon land step, existing FORK land
section. SuperGrok is paid. Meters stay distinct: included SuperGrok period
limits, SuperGrok dollar credits, console team prepaid / console API credits.

I did not run cargo or `just check`. I did not execute the assert script
(no shell in this implementer). Headings were checked by read against the
assert marker list.

## Files changed

Host (outside product git):

- `~/.agents/skills/git-recon/SKILL.md`
- `~/.agents/skills/upstream-export-import/SKILL.md`

Branch:

- `doc/dev/upstream-regression-filters.md`
- `docs/upstream-history.md`
- `scripts/recon-status.sh`
- `scripts/assert-process-pins.sh`
- `scripts/import-upstream-export.sh`
- `justfile`

Not touched:

- `FORK.md` (writer already reserved the catalog hook in Land checklist Rules)
- `AGENTS.md` (Survive-recon pointer already exists; no D1 novel)

## Host git-recon / export-import

Six product classes became **seven**. Class 7 is product skills are not a
Python runtime. Closeout must refuse chrome-only. Paint-only bubble copy is
a failed land. Skills Python reintroduced is a failed land.

Land walks extra neighbors **the catalog already lists** (bubble click, plan
present is not Approve, SHA-aware `/rebuild`, nucleo reuse-per-root,
`from_config` cold catalog). That is not a second numbered board.

Always-three-layer: L2 always spawns L3 for land `rg`/cargo, conflict
resolve, and CI red. The old "L2 must spawn L3 on many greps" line is gone
from both skills.

## What you enrolled

Proven named tests added to the existing catalog (class tables, extra
neighbors, operator cheat sheet). Not a second inventory.

**Class 1.** `user_guide_operator_cli_examples_use_grok_oss`,
`welcome_badge_brands_grok_oss`, `hero_subtitle_brands_grok_oss`,
`tutorial_list_title_brands_grok_oss`

**Class 5 cargo / cheat sheet (already in the class 5 table).**
`align_after_billing_switches_sticky_personal_full_to_business_included`,
`prepare_sampler_for_turn_aligns_to_ranked_included_primary`,
`resolve_model_to_sampling_config_auto_use`

**Class 6.** `materialize_new_auto_stays_welcome_when_no_last_session`,
`materialize_new_auto_does_not_open_last_when_headless`,
`from_pager_args_opens_last_session_on_start`

**Neighbors (walk if listed; not classes 8 through 14).**

- Plan honesty: `exit_plan_mode_present_is_not_operator_approve`,
  `exit_plan_mode_tool_result_does_not_claim_operator_approval`,
  `exit_plan_mode_keeps_mid_compose_draft_and_a_types`,
  `exit_plan_mode_modal_park_does_not_steal_mid_compose_keys`,
  `exit_plan_mode_empty_present_printable_goes_to_composer`
- Three-layer product: `child_task_description_is_concise`,
  `default_max_allows_l2_to_spawn_l3`
- `from_config_without_prefetch_produces_usable_catalog`
- SHA rebuild: `peer_relaunch_accepts_same_semver_different_sha`,
  `peer_relaunch_declines_equal_identity_on_same_path`,
  `peer_relaunch_accepts_deleted_inode_even_when_identity_equal`,
  `leader_is_older_than_same_semver_git_sha_identity`,
  `parse_version_output_extracts_identity`
- Nucleo: `repeated_open_without_close_keeps_one_search_per_root`,
  `distinct_roots_each_keep_one_search`,
  `get_results_does_not_keep_a_stale_search_alive`
- Pause / Clear finished:
  `work_control_chrome_matrix_pause_not_cancel_stop_not_pause`,
  `pause_button_click_dispatches_global_pause_not_cancel`,
  `clear_finished_action_idle_is_quiet_not_neon_green_or_magenta`,
  `clear_finished_click_does_not_open_subagent`
- Guide: `user_guide_does_not_claim_automatic_host_hop_is_unshipped`,
  `user_guide_names_token_economy_spend_order`

Class 3 catalog heading still says `grok-oss SQL extras` so the assert
title sniff stays stable. The body now says that extra SQL is the Token
Economy ledger, not SuperGrok dollar credits. FORK class 3 title (Token
Economy ledger) is the writer's wording.

## What you refused

**Catalog lies (no matching `fn`).** Removed from required-land cargo and
the cheat-sheet neighbor list. Marked honesty leftover. Kept still-present
neighbors (`stream_started_emits_retry_state_stream_resumed`, sampler
footer / timeout, branded `window_title_*` / `titles_on_session_*`).

- `retry_chrome_soft_reconnects_when_retry_stream_starts`
- `stream_resumed_without_prior_retry_clears_activity`
- `clip_retry_reason_*`
- `retrying_activity_label_*`
- `retrying_label_shows_timeout_*`
- `shell_collision_contract_covers_every_pager_command_and_alias`
- `default_title_items_include_agents`
- `title_escape_never_empty_payload`
- `title_updates_gated_only_by_title_enabled`

**Brittle assert.** Did not add a sibling script that scrapes every catalog
identifier and `rg`s `fn`. That would be a second test table and would
false-red on honest residual names or heading churn. Path assert now does
two real checks only:

1. `doc/dev/upstream-regression-filters.md` exists (not just `doc/dev`
   non-empty).
2. The seven product class titles still appear in that catalog (worktree
   and tree-ish).

No cargo inside assert.

**Also refused as land classes:** rustc 1.97.1 (file pin only), empty
`models_cache.json` miss (no `fn`), Nucleo pool `Some(2)` (constant only),
`/limits` hit-count cargo pin (prose only).

## recon-status / just / import NOTE / history

- `scripts/recon-status.sh` land next-action names assert **and** walk
  FORK/catalog named tests. `just check` is quality only. Unicode ellipsis
  in the UU overflow line became `...`.
- `just upstream-land-filters` runs the existing path assert, then prints
  the seven-class reminder. It does not replace `just check`. It does not
  run cargo.
- Import post-restore NOTE prints the seven classes and the catalog path.
  Review checklist item 2 matches. No new checklist file.
- `docs/upstream-history.md` HITL sequence and import review checklist
  dual-pin: walk FORK classes, prove catalog named tests exist, refuse
  chrome-only / paint-only bubble / skills Python.

## Whether you touched FORK.md

No. `.agents/reports/fork-docs-fork-write.md` exists. The writer already
put the catalog hook under Land checklist Rules. No extra hook added.
