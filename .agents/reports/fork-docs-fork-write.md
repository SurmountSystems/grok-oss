# FORK.md defense-inventory write (2026-08-15)

**File owned:** `/home/hunter/Projects/surmount/grok-build/FORK.md`  
**Sources:** `.agents/reports/fork-docs-map.md`, `fork-docs-seams.md`, `fork-docs-tools-map.md`, `fork-docs-process-gaps.md`, plus spot `fn` greps and `FORK_PATHS` in `scripts/import-upstream-export.sh`.

This turn did **not** run cargo or `just check`. Named tests were found in the tree (seams report + spot-check). SuperGrok is paid.

## What sections changed

Kept intact (not hollowed): **Vision**, **Git flow**, **Remotes**, **Syncing with xAI**. Em dashes in the Syncing job table were replaced with colons so the file meets the no-em-dash law.

Rewrote / expanded:

- **What Grok OSS adds** intro: now an operational defense-inventory rule (one line + named `fn`; enroll catalog; helper-green out).
- **### Product**: split dual-auth / meters / last-session / branding / `/spend` vs append log / `/rebuild` / `from_config` / nucleo / three-layer product prompt. Scrubbed "out of allowance", "included weekly", "free-period" as meter names (wire names stay after a plain thought). Renamed "grok-oss SQL extras" language to **Token Economy ledger / extra SQL** so it cannot be read as SuperGrok dollar credits.
- **### Chrome** (new): moved DOGE / rails / caret / titled composer / compact included SuperGrok period limits meter / five-CTA / plan honesty / bubble click+wrap / Clear finished / pause-resume-stop / hide_header / titles / activity glyphs out of the old Process dump. Each proven seam has a crate + `fn`.
- **### Packaging and build**: rustc 1.97.1 is now **file pin only; not cargo-proven**; `rust-toolchain.toml` is **not** in `FORK_PATHS`.
- **### Process**: slimmed. Chrome novels removed. Always-three-layer stays a process pin pointing at AGENTS; product `CHILD_TASK_DESCRIPTION` lives under Product.
- **### User-guide fork pins** (new): one line + link per page, not a page dump.
- **### Dogfood snapshot (2026-08-09)**: date-boxed so it cannot demote later Product / Chrome / Land claims. Explicit: no live TUI dogfood; do not start rebuild or quit.
- **### What recon keeps**: added a `rust-toolchain.toml` row (not path-restored).
- **How to defend a seam on the next merge** (new).
- **Land checklist**: steps vs seven **product** classes no longer collide. Class 3 title is Token Economy ledger `/spend`. Class 5-7 match the catalog (hop, last-session, skills-not-Python). Chrome-only / paint-only bubble / skills-Python = failed land. Extra proven classes listed separately.
- **Upstream regression filters cheat sheet**: expanded to proven extras; dropped catalog identifiers with **no matching `fn`**.

Unchanged after land: **CI and local quality**, **Versioning**, **Multi-session rate limits**, **Canonical repo**, **License** (em dash on the license line became a colon; GitHub rate-limit URL restored to `docs.github.com` after a write typo).

## What was added

Proven seams now have hierarchical FORK lines (crate + exact `fn`):

- Welcome / tutorial / hero `grok-oss` badges; operator CLI leftover guide test.
- Last-session siblings (Welcome / headless / ctx flag).
- Business/Team included before personal; sibling included before SuperGrok dollar credits; one-process limits flock; combined remaining; compact meter stays included.
- Bubble **click + wrap** (`clicking_*`, first-line-full).
- Plan present ≠ Approve + modal-free typing (exact names, not `exit_plan_mode_soft`).
- Pause / Clear finished paint + hit tests.
- `/rebuild` SHA-aware peer relaunch (not only fail-does-not-signal).
- `from_config_without_prefetch_produces_usable_catalog`.
- Nucleo reuse-per-root (three `fn`s).
- Always-three-layer product: `child_task_description_is_concise`, `default_max_allows_l2_to_spawn_l3`.
- User-guide hop-shipped + spend-order cargo pins.
- Land extra-class cargo blocks for all of the above.
- Catalog hook sentence for the process improver (see below).

## What you refused to claim

- Live TUI / dogfood. No rebuild, no quit.
- Fresh cargo green. Tests were found, not run.
- rustc 1.97.1 is cargo-proven. File pin only. `flake.nix` is in `FORK_PATHS`; `rust-toolchain.toml` is not.
- Empty `models_cache.json` miss is cargo-proven. Only the no-prefetch usable-catalog `fn` is.
- Nucleo pool `Some(2)` is cargo-proven. Reuse-per-root is. Pool size is a constant.
- Token Economy / economic-mode / auto-run `/settings` table rows (seams walk did not re-prove them).
- Session recap / cancel-subagents Settings rows as e2e-proven.
- Stuck-retry **pager** chrome is catalog-proven. Shell emit + sampler footer/timeout exist. Named pager chrome `fn`s are gone.
- Catalog / old cheat-sheet identifiers with **no matching `fn`**: `retry_chrome_soft_reconnects_when_retry_stream_starts`, `stream_resumed_without_prior_retry_clears_activity`, `clip_retry_reason_*`, `retrying_activity_label_*`, `retrying_label_shows_timeout_*`, `shell_collision_contract_covers_every_pager_command_and_alias`, `default_title_items_include_agents`, `title_escape_never_empty_payload`, `title_updates_gated_only_by_title_enabled`.
- Lower-left throbber **color** `fn`s (`doge_idle_subagent_still_running`, `doge_tool_running_spinner` still absent).
- User-guide last-session sentences and three-layer paragraph as cargo-pinned (text exists; no dedicated `fn`).
- `/limits` hit-count as cargo-proven (catalog prose only).
- Host overlay skills (`~/.agents/skills`) as a product land class.
- SuperGrok as free. "included weekly", "out of allowance", or "SQL extras" as meter names.

FORK-claims that the seams walk did not re-prove stay labeled **FORK claims** / residual-aligned (UDAX, ULID, OpenRouter multi-key, keyring, auto-compact, auto-run implement, continue-interrupted, multi-track, click-tasks, Worked-for, etc.). They are not land classes.

## Reserved hook for the process improver

In **Land checklist → Rules**:

> **Catalog hook (process improver):** recon agents walk these classes in `doc/dev/upstream-regression-filters.md`. A sibling name-existence check (not this file, not `REQUIRED_FILES`) may later fail if a required-land identifier has no matching `fn`. Path assert stays files-only.

Also pointed from **How to defend** item 3: the process improver owns the catalog and the assert / git-recon wiring.

FORK class numbering now matches the catalog's seven product classes. Host `git-recon` still saying six classes, `recon-status.sh` land next-action, and a sibling `assert-land-catalog-fns` script were **not** edited (out of scope).

End of write report.
