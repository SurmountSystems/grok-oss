# Pin: one reviewer, document tasks (2026-08-15)

Operator killed / demanded stop of three parallel "Review edit-verify …"
agents. Those were one effort-3 review split into product / tests / plan
alignment. That looked like three copies. All three cancelled.

## Law now on disk

- Project `AGENTS.md` 3d: one review job per slice unless the operator
  explicitly asked for more than one.
- Project `AGENTS.md` 3e: document every live task on disk before
  compaction. Chat is not enough.
- Host `~/.grok/AGENTS.md`: same two subsections.
- Implement skill: `--effort 3` must not spawn three visible Review
  agents on Surmount.

## Not started this turn

ACP file-lock plan stays pending. No new agents after this pin.
