# Residual inventory (hourly loop) — 2026-08-07 (refresh 21:43)

Source: `RESIDUAL.md` Open + Highest-value next.

## Top 5 agent-doable (ranked)

1. **Multi-track “also” product guard** — Bind live subagents to board todos; block demote of a live track without explicit cancel; optional sticky “N agents running.” Process pins alone failed (`feat:multi-track-also-guard`). **No product plan yet:** short plan under `.agents/plans/` first if picked (do not invent full ship).
2. **Optional Management process-cache polish** — Only if dogfood still wants more than force-refresh + 60s TTL (already shipped). Soft rank.
3. **Structured token-efficient conversation plan** — Plan only (`plan:structured-token-efficient-convo`). Do not invent product shape.
4. **ASCII scrub / SX free polish** — `…` → `...` if free with another edit; no bulk SX steers.
5. **Plan freeform `plan.md` menus** — Process/skills only if dogfood still jars; product chrome green. Behind operator ask.

## Operator-gated only

| Item | Why human |
|------|-----------|
| Rebuild + dogfood full limits stack on live binary | Trust multiproc, durable history, series, force-refresh together |
| C4 / free SuperGrok period debit ticket (Item 2) | Server-side; evidence package ready; **do not invent debit** |
| Live extras-after-full (Item 3 / C5) | Needs free period ≥ 100% with positive SuperGrok $ extras |
| Live multi-poll flat note | Needs healthy session billing + long-lived process |
| Onto join / PR / import ledger / git land | Human TTY; agents never `git commit` |
| Secrets, management key install, ticket filing | Human / xAI |

## Closed / shipped recently

| Topic | Status |
|-------|--------|
| **Phase R rate limits by API type** | Shipped 2026-08-03; verified 2026-08-07 |
| **Sibling poll N consecutive auth-fail demote** | Shipped 2026-08-07 20:43 |
| **Multi-slot OIDC refresh before sibling poll** | **Shipped 2026-08-07 21:43** — ensure_fresh + persist multi-slot only |

## Billing / chrome related (status)

| Topic | Status |
|-------|--------|
| **Limits before credits (Design A)** | Shipped; compact `N%` with free-period headroom is intentional |
| **Dual SuperGrok billing soft edges** | **Closed** (Option B + N-fail demote + multi-slot OIDC refresh) |
| **Half B team meters** | Core prepaid/postpaid/series text shipped; license charts non-goal |

## Do not invent

SuperGrok included debit (C4), browser license charts as SuperGrok proof, folding default credits into team prepaid `$N`, re-opening Phase R or dual-poll soft edges without new evidence.
