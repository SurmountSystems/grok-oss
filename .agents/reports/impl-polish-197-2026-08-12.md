# Report: leftover 1.97 polish (docs + same-class clippy)

**Date:** 2026-08-12
**Branch:** onto-xai/b13fa526f511
**Role:** low-priority polish implementer
**No full `just check` / `just ci`.** Operator owns that run (nextest was live the whole time).

## Honesty (docs)

### `RESIDUAL.md`

Open section did **not** still list the finished 1.97 / CI unit mop as work to do. Added a short shipped note so compaction does not re-open it:

- rustc / fenix **1.97.1** pin
- unit clusters greened this wave (worktree/export hermetic git, team-managed dark seam, theme+hooks, shell stack/interject/usage/stream/external-auth)
- package clippy mop for shell / pager / tools / update / workflow already green under `-D warnings`

Still called out as **not this mop:**

- operator dogfood after install (existing first Open bullet)
- PTY grandchild-kill flake reliability (`close_pty_kills_a_background_grandchild`)
- a later second onto/join after this mop is finalized (do not start one from residual)

Did not invent non-goals. Did not dump recon diaries into D1.

### `FORK.md`

Was silent on the toolchain pin (no "we pin 1.94" claim). Added a short standing packaging note: Surmount **1.97.1** wins even if an upstream export still lists 1.94.x, unless the operator chooses a different channel. Pointer: `.agents/reports/impl-toolchain-1971-2026-08-12.md`.

### `AGENTS.md`

No toolchain sentence that still said 1.94. No dual-pin.

## Leftover same-class clippy

`just test-clippy` is workspace `--lib --bins -D warnings`. The operator's `just check` was already in **nextest** when this run started, so that clippy step had already passed on the pre-edit tree. Remaining work was the same mechanical class already accepted elsewhere, scanned in crates that were not the earlier shell/pager/tools mop.

Touched only `sort_by` / `sort_unstable_by` that is a single key (including `Reverse` on Copy keys). Did **not** rewrite `then_with` chains, `partial_cmp` / `total_cmp` float sorts, or guarded `Ok`/`Err` matches that are not `?`.

Did **not** weaken or rewrite test contracts.

### Files

| Package | File | Change |
|---------|------|--------|
| `xai-grok-workspace` | `src/foreign_sessions/codex/files.rs` | `dates.sort_by(\|a, b\| b.cmp(a))` → `sort_by_key(\|&d\| Reverse(d))` |
| `xai-codebase-graph` | `src/scope_graph/graph.rs` | definition pick: `sort_by` on `byte_size()` → `sort_by_key` |
| `xai-grok-agent` | `src/prompt/agents_md.rs` | rules `*.md` paths: `sort_by` on `file_name()` → `sort_by_key` (owned `OsString`) |
| `xai-grok-agent` | `src/plugins/registry.rs` | plugin list: `(scope, name)` `sort_by` → `sort_by_key` |
| `xai-grok-sandbox` | `src/hook_write_deny.rs` | snapshot files: `sort_by` on `path` → `sort_by_key(\|f\| f.path.clone())` |
| `xai-computer-hub-sdk` | `src/metric_donate.rs` | Prometheus labels: `sort_by` on `key` → `sort_by_key(\|a\| a.key.clone())` |
| `mermaid-to-svg` | `src/svg_renderer.rs` | layout nodes: `sort_by` on `id` → `sort_by_key(\|a\| a.id.clone())` |
| `dagre_rust` | `src/layout/order/sort.rs` | unsortable: `sort_by` reverse `i` → `sort_by_key(\|e\| Reverse(e.i))` |
| `dagre_rust` | `src/layout/order/cross_count.rs` | south edges: `sort_by` on pos → `sort_by_key(\|e\| e.0)` |
| `dagre_rust` | `src/layout/position/bk.rs` | neighbor order: `sort_by` on `pos` → `sort_by_key(\|w\| pos.get(w).copied().unwrap())` |
| `dagre_rust` | `src/layout/order/init_order.rs` | simple nodes: rank `sort_by` → `sort_by_key` |

Left alone on purpose (not the accepted mechanical class, or already green under `-D warnings`):

- `then_with` multi-key sorts (shell, pager, workspace foreign-session summary, mermaid sequence/sankey)
- `partial_cmp` / `total_cmp` float sorts (memory search, mermaid layout, pager-render gboom, pty-harness)
- `Ok`/`Err` matches with a NotFound / retry / convert-error arm (not `?`)
- enum variant `None` arms (`GraphicsProtocol::None`, `NotificationProtocol::None`)
- `question_mark`, `useless_borrows_in_formatting`, `manual_checked_ops`, `collapsible_match`, `0.1_f32` leftovers: no remaining lib/bin sites of the same accepted shape after the earlier mop

## Left open (not done here)

1. **Second onto / join.** Operator-owned. Do not start it from this polish.
2. **Dogfood.** Install + quit old TUIs + reopen `grok-oss`. Still the first real Open bullet.
3. **PTY flaky.** `close_pty_kills_a_background_grandchild` is still reliability residual. During this run the operator's nextest had that exact test live for many minutes (PID still present after 12+ minutes of nextest). Not treated as a new product bug and not rewritten.

## Commands + exit codes

| Command | Exit |
|---------|------|
| `cargo fmt -p xai-grok-workspace -p xai-codebase-graph -p xai-grok-agent -p xai-grok-sandbox -p xai-computer-hub-sdk -p mermaid-to-svg -p dagre_rust` | **0** |
| `cargo clippy -p mermaid-to-svg --all-targets -- -D warnings` | **0** |
| `cargo clippy -p dagre_rust --all-targets -- -D warnings` | **0** |
| `cargo clippy -p xai-grok-sandbox --all-targets -- -D warnings` | **0** |
| `cargo clippy -p xai-codebase-graph --all-targets -- -D warnings` | **0** |
| `cargo clippy -p xai-computer-hub-sdk --all-targets -- -D warnings` | **0** |
| `cargo clippy -p xai-grok-agent --all-targets -- -D warnings` | **0** |
| `cargo clippy -p xai-grok-workspace --all-targets -- -D warnings` | **0** |

Clippy ran at `nice -n 19 ionice -c3` after nextest had released the cargo lock (test-run phase). Did not run full `just check`.
