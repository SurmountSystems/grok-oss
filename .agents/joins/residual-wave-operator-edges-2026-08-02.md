# Join: RESIDUAL Open update after operator-edges residual wave

**Date:** 2026-08-02
**Scope:** D0 residual docs only (`RESIDUAL.md` Open §4 + Highest-value next + Validate)
**No product code. No git.**

## Inputs (not re-proved)

| Source | Role |
|--------|------|
| `.agents/joins/c4-supergrok-debit-evidence-package-2026-08-02.md` | Server ticket brief; C4 still FAIL; no invent debit |
| `.agents/joins/impl-prepaid-cache-ttl-polish-2026-08-02.md` | Prepaid TTL + force clear on explicit limits collect + lag note shipped |
| `.agents/joins/impl-f1b-attribution-soft-2026-08-02.md` | F1b soft product honesty complete (close-out; no code) |
| `.agents/joins/live-multipoll-flat-note-2026-08-02.md` | Live multi-poll still blocked (auth fail / cold process; `flat_poll` absent) |
| Prior residual join | `.agents/joins/residual-edges-update-2026-08-02.md` |

## Explicit honesty pins held

- **Limits before credits** always (Design A).
- Meters stay distinct: SuperGrok **included weekly** ≠ SuperGrok **$ extras** ≠ console team **prepaid** ≠ console team **postpaid OAuth/API class**.
- **Do not invent C4 SuperGrok debit** as proven or "fixed." Evidence package ≠ debit closed.
- Default `auto_use_included_limits` **not flipped** (operator-gated).
- Product client invent for limits-first is **largely exhausted**.
- C5 still code-only (included never ≥ 100% live).

## Shipped this wave (from joins)

1. **C4 evidence package** on disk (ticket-ready brief for human/xAI). C4 still FAIL.
2. **Prepaid cache TTL polish** (product + review): force clear on explicit `grok limits` collect; lag note (process cache + last-good); shared TTL const.
3. **F1b soft** already product-complete; close-out join with green unit evidence.
4. **Live multi-poll** attempted; still blocked (billing auth fail; `flat_poll` / Build % absent on dumps).

## Still open (honest)

| Item | Status |
|------|--------|
| Default `auto_use_included_limits` for new installs | Operator-gated; **not flipped** |
| C4 server-side | Evidence ready; debit not proven; ticket is human/xAI |
| Live multi-poll flat note | When session billing healthy + long-lived process |
| M6 series / defaultCredits | Optional only if operator wants |
| Optional TUI force-refresh parity with CLI | Soft; only if dogfood wants |
| Intentional image/voice/BYOK / `api_key` pin bypasses | Documented; not claimed fixed |

## RESIDUAL.md edits (list)

### §4 status header

- Status line now includes operator-edges residual wave: C4 evidence package ready, prepaid TTL shipped, F1b soft close-out done, live multi-poll still blocked; product client invent largely exhausted.
- Debit non-claim: multi-poll auth-fail / cold process called out; `flat_poll` absent.

### §4 Still open (rewrote honestly)

| Item | Change |
|------|--------|
| **C4 / branch 2b** | Still FAIL. Links C4 evidence package join. Filing is human/xAI, not more client invent. |
| **Default auto_use** | Still open; **not flipped**; ranked as highest-value client remaining decision. |
| **Live multi-poll flat note** | Still open; links blocked attempt (auth fail, `flat_poll` absent). Retry only when session billing healthy. |
| **M6 / defaultCredits** | Unchanged optional if operator wants. |
| **Optional TUI force-refresh parity** | New soft open (CLI already force-busts; TUI background TTL). |
| **F1b** | Product honesty **complete** (close-out join); soft leftover browser $547 vs M3 window only. Not re-opening rank policy. |
| **Prepaid TTL** | Stays as **shipped** bullet (not open work). |
| **Bare resolve intentional bypasses** | Unchanged soft remaining. |

### §4 Highest-value next (re-rank after operator-edges)

Old ranks that treated C4 package + prepaid TTL + F1b as open product work retired for those shipped / package-ready items.

New order (honest: operator first, client invent largely exhausted):

1. Operator decision: default `auto_use_included_limits=true` for new installs
2. Optional TUI force-refresh parity with CLI (if dogfood wants)
3. Optional live multi-poll flat note when session billing healthy + long-lived process
4. C4 remains server-side (evidence ready; no invent debit)
5. M6 series / defaultCredits only if operator wants

### Global Highest-value next table

- Summary: operator-edges wave shipped/package-ready; C4 still FAIL; multi-poll blocked; still-open list re-ranked.
- Parallelization note: prepaid TTL + F1b soft + C4 package **done** (package ≠ debit); remaining mostly operator-gated or server-side.
- Rank table rewritten (auto_use first; TUI refresh; multi-poll; C4 server; shipped prepaid + F1b struck; M6; plan freeform).
- "Not parked forever" lists prepaid TTL + F1b soft complete + C4 evidence assembled.

### Validate honesty

- **2j** new: prepaid TTL / force-refresh cargo filters.
- **2k** new: F1b attribution soft honesty cargo filters (from F1b close-out join).
- **2g** live dogfood: links multi-poll attempt + C4 evidence package; retry only when session billing healthy on long-lived process.

## Explicit non-claims

- Did **not** invent C4 SuperGrok included debit as closed or fixed.
- Did **not** claim C4 ticket filed with xAI (package only).
- Did **not** claim live multi-poll flat note proved on a healthy long-lived process.
- Did **not** claim C5 live (included never ≥ 100%).
- Did **not** claim default `auto_use` flipped.
- Did **not** claim image/voice/BYOK public-host bypasses "fixed."
- Did **not** edit product code or run git.
- Did **not** move lasting product truth to FORK this turn (D0 residual honesty only).

## Files touched

- `/home/hunter/Projects/surmount/grok-build/RESIDUAL.md` (Open §4, Highest-value next, Validate)
- This join: `.agents/joins/residual-wave-operator-edges-2026-08-02.md`
