# Join: RESIDUAL Open update after residual-edges wave

**Date:** 2026-08-02
**Scope:** D0 residual docs only (`RESIDUAL.md` Open §4 + Highest-value next + Validate)
**No product code. No git.**

## Inputs (not re-proved)

| Source | Role |
|--------|------|
| `.agents/joins/impl-branch-2b-honesty-2026-08-02.md` | Soft honesty polish shipped |
| `.agents/joins/impl-bare-resolve-console-edge-audit-2026-08-02.md` | Bare resolve landmines closed; intentional bypasses |
| `.agents/joins/live-limits-recheck-2026-08-02.md` | Log weak 65→66; tip sparse; C4 not closed; C5 not live |
| `.agents/joins/slice2-dogfood-g4-2026-08-02.md` | Morning C4 fail / branch 2b baseline |
| Prior residual join | `.agents/joins/residual-limits-first-update-2026-08-02.md` |

## Explicit honesty pins held

- **Limits before credits** always (Design A).
- Meters stay distinct: SuperGrok included weekly ≠ SuperGrok $ extras ≠ console team prepaid ≠ console team postpaid OAuth/API class.
- **Do not invent C4 SuperGrok debit** as proven or "fixed."
- Soft honesty polish ≠ server debit fixed.
- Default `auto_use_included_limits` **not flipped** (operator-gated).
- C5 still code-only (included never ≥ 100% live).

## RESIDUAL.md edits (list)

### §4 status header

- Status line now: Slices 1/3/4 + residual-edges wave (soft honesty + bare-resolve audit) shipped; **C4 still FAIL** (branch 2b, server-side).
- Debit non-claim updated: product honesty holds; weak log 65→66; Build still flat 54; tip CLI sparse.

### §4 shipped block (limits-first campaign)

Added two shipped bullets under **Also shipped (limits-first campaign — 2026-08-02):**

1. **Residual-edges wave — branch 2b soft honesty polish**
   Dynamic flat-poll note (only meters observed flat); Build % on `/limits` and non-silent `/usage`; C6 without proving included moved; doctor after-burner pin; sibling Build plumbing. Join: `impl-branch-2b-honesty-2026-08-02.md`.

2. **Residual-edges wave — bare resolve / console-edge audit**
   `ModelsManager::sampling_config`, subagent override (fail-closed), `resolve_model_to_sampling_config` all use rank. Intentional image/voice/BYOK/OpenRouter/`api_key` pin bypasses documented. Default `auto_use` not flipped. Join: `impl-bare-resolve-console-edge-audit-2026-08-02.md`.

### §4 Still open (rewrote honestly)

| Item | Change |
|------|--------|
| **C4 / branch 2b** | Still FAIL, server-side. Product honesty shipped, not "fixed debit." Morning flat + log weak 65→66 + Build 54 + tip sparse. Links live recheck + soft honesty joins. Optional: ticket evidence package. |
| **F1b** | Kept as soft attribution residual (M3 + C6 shipped). |
| **Default auto_use** | Still open; **not flipped** this wave; call sites honor flag when on. |
| **Bare resolve** | Primary landmines **closed**; intentional media/BYOK bypasses remain soft remaining only (not bugs). |
| **Soft OAuth honesty polish** | **Removed from open** (shipped this wave). |
| **Optional live multi-poll flat note** | New open: code path shipped; live proof when billing polls work again. |
| **M6 / defaultCredits** | Unchanged optional. |
| **Prepaid cache TTL** | Still soft polish; optional force-refresh only if operator wants. |
| **TUI postpaid cache** | Unchanged soft. |

### §4 Highest-value next (re-rank after residual-edges)

Old rank (2b honesty + bare resolve as product work) retired for those two (shipped).

New order (honest: mostly operator/server, not client invent):

1. Operator decision: default `auto_use_included_limits=true` for new installs
2. Server-side C4 ticket evidence package (optional; no invent debit)
3. Soft prepaid cache TTL / force-refresh polish (optional)
4. Optional live multi-poll flat note when billing works
5. F1b attribution soft
6. M6 / defaultCredits only if wanted

### Global Highest-value next table

- Summary paragraph: residual-edges shipped; C4 still FAIL with weak 65→66; tip sparse; still-open list updated.
- Parallelization note: soft honesty + bare-resolve **done**; remaining items mostly operator-gated or server-side.
- Rank table rewritten to ranks 1–7 matching §4 re-rank (auto_use, C4 ticket package, prepaid TTL, live multi-poll, F1b, M6, plan freeform).
- "Not parked forever" lists residual-edges soft honesty + bare resolve as **shipped**.

### Validate honesty

- **2h** new: soft honesty polish cargo filters (from branch-2b join).
- **2i** new: bare resolve rank wire-up cargo filters (from bare-resolve join).
- **2g** live dogfood: links live recheck join; notes dynamic flat note + Build % surfaces; still no invent C4.

## Explicit non-claims

- Did **not** invent C4 SuperGrok included debit as closed or fixed.
- Did **not** claim C5 live (included never ≥ 100%).
- Did **not** claim default `auto_use` flipped.
- Did **not** claim image/voice/BYOK public-host bypasses "fixed."
- Did **not** edit product code or run git.
- Did **not** move lasting product truth to FORK this turn (D0 residual honesty only).

## Files touched

- `/home/hunter/Projects/surmount/grok-build/RESIDUAL.md` (Open §4, Highest-value next, Validate)
- This join: `.agents/joins/residual-edges-update-2026-08-02.md`
