[package]
license = "Apache-2.0"
name = "surmount-coordinator-gui"
version = "0.1.0"
edition.workspace = true
description = "Laptop L0 coordinator: safe /running JSON and set-remote-host-console-api-key (never prints the key)"
publish = false

[[bin]]
name = "surmount-coordinator-gui"
path = "src/main.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
