[package]
license = "Apache-2.0"
name = "surmount-coordinator-gui"
version = "0.1.0"
edition.workspace = true
description = "Application state and safe /running JSON for the Surmount GPUI L0 window"
publish = false

[[bin]]
name = "surmount-coordinator-gui"
path = "src/main.rs"

[dependencies]
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }

[lints]
workspace = true
