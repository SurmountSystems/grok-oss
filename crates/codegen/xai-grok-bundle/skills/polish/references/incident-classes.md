# Incident classes (polish hunt)

Classes of polish nits, not a session diary. Hunt these from chat and
Grok OSS screenshots. SuperGrok is a paid product. Never call SuperGrok
free. grok-oss chrome is a client printout.

## Billing honesty

Name the meter in complete thoughts:

- included SuperGrok period limits for the current billing period (how
  much of that included quota is already used)
- SuperGrok dollar credits (prepaid top-ups on the SuperGrok account)
- console team prepaid / console API credits

Do not invent remaining SuperGrok. Do not call any pool used up unless
the live product Usage view or `/limits` surface they can see agrees.
Fail-open. Limits tool naming the wrong meter. Team HTTP 403 while
included SuperGrok period limits chrome still has room. SuperGrok Heavy
is a distinct weekly pool from standard SuperGrok.

## Compact occupancy

Occupancy chip vs compact report. Compact late past the sampling
window. Chip still showing a pre-compact count after a successful
compact.

## Nested-agent reliability

Sluggish nested agents. A preparing write that never finishes. Missing
specialist reports under `~/.agents/reports/`.

## Chrome paint

Contrast. Scroll ceiling (cannot scroll past the first human line or
image). Timestamps on but not next to short lines. Glued timers
(retry countdown stuck to the model-turn timer).

## Effort persist

Composer effort resets to a default after the user set a different
level.
